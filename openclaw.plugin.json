{
  "id": "openclaw-askred",
  "name": "AskRed",
  "description": "Chat with your OpenClaw agents from the AskRed mobile app",
  "kind": "channel",
  "channels": ["askred"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "relay": {
        "type": "string",
        "description": "WebSocket relay URL",
        "default": "wss://multibot-relay.fly.dev"
      },
      "agents": {
        "type": "array",
        "description": "Agents to expose to the AskRed app",
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Agent identifier"
            },
            "name": {
              "type": "string",
              "description": "Display name in the app"
            }
          },
          "required": ["id", "name"]
        }
      }
    }
  }
}
